build/
sdkconfig
sdkconfig.old
managed_components/
dependencies.lock
.DS_Store
*.swp

# Contains Wi-Fi credentials + server URL; use nvs_config.example.csv as the template.
# Generated via the esptool path in docs/PROVISIONING.md.
nvs_config.csv
nvs_config.bin

# Auto-publish destination (scp target for the firmware bin); use
# .publish.env.example as the template.
.publish.env

