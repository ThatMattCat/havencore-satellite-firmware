## IDF Component Manager Manifest File
dependencies:
  chmorgan/esp-audio-player: "1.0.6"
  chmorgan/esp-file-iterator: "1.0.0"
  # esp-box-3 1.1.3 uses the legacy esp_lcd_new_panel_io_i2c_v1 path, which
  # rejects the scl_speed_hz field that esp_lcd_touch_gt911 1.2.x adds to
  # ESP_LCD_TOUCH_IO_I2C_GT911_CONFIG(). Pin below 1.2 until the BSP moves
  # to the new i2c_master driver.
  espressif/esp_lcd_touch_gt911: "<1.2.0"
